# NOTE(gap): Algorithm modules designed in 04-算法Pipeline设计.md
# NOT implemented — all algorithms are stubs.
# Phase 3 will deliver: pitch detection (CREPE), beat tracking (madmom),
# MIDI quantization, chord detection, accompaniment generation, score conversion.
#
# Current task creation only stores DB records — no actual processing occurs.
# See workers/ and algorithms/ for planned Celery integration.
