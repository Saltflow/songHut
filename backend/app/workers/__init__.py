# NOTE(gap): Celery worker integration designed in 03-后端模块设计.md Section 7.2
# NOT implemented — directory is empty.
# Celery is listed as a dependency in pyproject.toml but never imported.
# Tasks are created as DB records but never dispatched to workers.
# Phase 3 will add: celery_app.py, tasks.py (humming_to_melody_task, etc.)
